name: first-workflow
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "hello world"
      - name: second step
        run: echo "lets make it happen"
